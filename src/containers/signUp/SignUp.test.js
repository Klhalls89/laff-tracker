import React from 'react';
import SignUp from './SignUp'
import { isMainThread } from 'worker_threads';

describe('SignUp', () => {

    describe('handle input change', () => {

        it ('should set state with new name and value', () => {
            
        }) 

    })

    decribe('handle submit', () => {

    })
})